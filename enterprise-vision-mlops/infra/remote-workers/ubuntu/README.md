# Ubuntu Worker

`ruma-ubuntu` is a Tailscale-connected Linux worker candidate.

Recommended responsibilities:

- Docker runtime validation
- Linux batch preprocessing worker
- Future k3s worker or GPU node, if hardware and SSH access are available

Initial 2026-06-18 probe showed the node online over Tailscale but refusing SSH.
