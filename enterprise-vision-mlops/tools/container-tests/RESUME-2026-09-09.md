# API recovery and actual smoke follow-up

Container test-tool transition: **COMPLETE for the bounded synchronous API lane**.
Project decision remains **GLOBAL NO-GO / r7s5 incomplete / R8_NOT_STARTED**.
This follow-up does not overwrite the original CT-M06/CT-M07 blocked receipts or old R7 failures.

## Actual outcome

- CT-R01 confirmed that the Linux API used the default Windows registry path.
- CT-R02 applied only the two approved API environment settings. The original image
  `sha256:4570d7208b55459eface30dea69a4c6ef05d6770e6b6a5bf3e8de1acd836c35a`, model,
  release, authentication, ports, networks and mounts were preserved; no build/pull/migration.
- API container changed from `176c36e852a4...` to `820ec1a7f334...`; other 17 containers
  retained their IDs/names/states. Schema/task authority/idempotency/lease/effect rows were
  unchanged. Existing task mirror payload remained equal; only permitted version/timestamp
  and JSON rewrite occurred. The existing worker also refreshes this mirror, so the observed
  version increase is not attributed solely to API startup.
- CT-R03 health/catalog both 200, five existing registry artifacts hash-matched, original
  root model `vision-baseline` / Shadow / 18 and release `b9140ad...` retained.
- CT-R04 actual run `ct-model-smoke-20260908-01`: **3 attempted / 3 accepted / 3 succeeded**;
  failed/rejected/unknown/missing/skip/deselect all 0; driver/container exit 0.
- Exactly six required spans per request (18 total), API and Prometheus cumulative counters
  3, six terminal executor gauges 0. Before-run labeled counter samples had not been created;
  their absence is preserved, not represented as an observed numeric zero. Increase by 3 is
  inferred from the fresh API instance, exact request/trace accounting and cumulative read-back.
- Response p50 12.654939 ms, p95/p99 23.021896 ms; end-to-end p99 23.413802 ms;
  generator lag max 2.189398 ms. Physical Windows host CPU max 38% across 14 samples.
  These three low-load requests are not production performance/capacity certification.
- Owned test containers removed after log/output capture; residue 0. No service/data deletion.

The test image remains the already validated local immutable ID
`sha256:2c7a0ee83cc1f33740a480f6ee8d123a125de2c14bdeb985394d9e5b272605be` (0.1.0).
No image rebuild, new dependency installation, full focused suite or Windows gate was used
to earn the actual API smoke result. Prior two-container 10/10 TOOL_ONLY checks remain separate.

## Evidence and exact configuration

Evidence root:

```text
F:\EnterpriseMLOps_Data\enterprise-vision-mlops\artifacts\scale_validation\private\s8-v4\pre-r8-continuation-20260908T194638Z
```

Use `ct-r02-readback-amendment.json`, `ct-r03-v2-receipt.json`,
`ct-r04-corpus-receipt.json`, `ct-r04-profile-receipt.json`, and
`ct-r04-actual-smoke-receipt.json`. Raw response bytes, JIT-created profile/corpus hashes,
driver logs, resource samples and matched spans are stored under the run/observer directories.

Profile: `capacity-probe-low-load-smoke-20260908-01`; SHA256
`4324850f75b2c7b30e694df68ea12c21e54e84454aaec9b433a1a56672a41c0f`.
Client/source snapshot at execution: `7c5fab3d2919f184fa11c7f53b414c3582ed94af`.
Checkout HEAD: `ae9c014fba1ffe4891c1e5835d7ca94595846683`.
Read-only mounted executable hashes stayed equal to the previously checked tool code.

The Compose service is `api` in project `enterprise-vision-mlops`; `evm-api` is the container
name. Existing base/previous provenance override are recorded in `p0-receipt.json`.
Append `compose.api-smoke-migration-guard.yml`, then `compose.api-smoke-registry.yml`.
The first keeps `EVM_CONTROL_PLANE_AUTO_MIGRATE=false`; schema verification remains mandatory.
The second sets `/mnt/evm-data/artifacts/scale_validation/s3/capacity-registry.json`.
No extra mount was necessary. Recovery removes only the registry overlay while retaining
the migration guard, using the original image and `up -d --no-deps --no-build --pull never
--force-recreate --wait --wait-timeout 60 api`. Do not execute recovery without a real regression.

The first recreation recorder failed after the Compose command because bind ordering and
DNS empty-array/null representations differed. Independent read-back verified semantic equality
and healthy state. The Compose exit code was not persisted and is explicitly UNKNOWN, not
invented from its Healthy message; the original recorder exit 1 and logs are retained.
No duplicate recreation was performed. Subsequent readiness and actual-smoke exits are recorded.

## Reproduction and next work

To inspect this completed run (PowerShell):

```powershell
$preEvidence = 'F:\EnterpriseMLOps_Data\enterprise-vision-mlops\artifacts\scale_validation\private\s8-v4\pre-r8-continuation-20260908T194638Z'
Get-Content -Raw "$preEvidence\ct-r04-actual-smoke-receipt.json" | ConvertFrom-Json
Get-Content -Raw "$preEvidence\ct-model-smoke-20260908-01\result.json" | ConvertFrom-Json
```

Only when a new smoke is required, first recheck unchanged API/model/code/input identities and
capture a fresh telemetry baseline, then use a new run ID and the same tested image. Example
new-run command (not automatically executed by this report):

```powershell
& 'F:\r7-m03-pythonbase-ace23615-01\python.exe' -B tools/container-tests/run_container.py --run-id ct-model-smoke-20260908-02 --input-directory "$preEvidence\ct-smoke-inputs-02" --output-root $preEvidence --network evm-local --timeout 90 --docker 'C:\Program Files\Docker\Docker\resources\bin\docker.exe'
```

Continue automatically with the current pre-r8 directive: P2 mapping, minimal unresolved R7
fixture/product fixes, current-lock static/focused/full/fresh and required environment lanes,
then aggregate/closure only when all required results exist. The old EXECUTION05 5 failures,
stale lane results, Windows Job and real-PG/CUDA/private obligations are not replaced by Docker.
Do not initiate R8 ETW/dual collector/fault injection, V4 full stack/Q0, 54/78, soak or Integrated V4.
